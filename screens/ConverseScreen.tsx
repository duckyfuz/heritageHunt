import { SafeAreaView } from "react-native-safe-area-context";
import { Text } from "react-native-paper";

const ConverseScreen = () => {
  return (
    <SafeAreaView>
      <Text>ConverseScreen</Text>
    </SafeAreaView>
  );
};

export default ConverseScreen;
